extensions:
  - (docker): Dockerfile

dependencies:
  - ../src

steps:
  docker:
   - (docker): build
     configuration: $(configuration)

  push:
   - (docker): push
     tag: ghcr.io/magnus-opera/project3:666
