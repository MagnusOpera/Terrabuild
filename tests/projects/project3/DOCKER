extensions:
  - (docker): Dockerfile

dependencies:
  - DOTNET

steps:
  docker:
   - (docker): build
     configuration: $(configuration)

  push:
   - (docker): push
     tag: ghcr.io/magnus-opera/project3:666
