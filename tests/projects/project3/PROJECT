dependencies:
  - ^dotnet: project3.csproj

targets:
  build: [^build]

outputs:
  - bin
  - obj

steps:
  build:
    - ^shell: echo
      args: "building project3"
    - ^dotnet: build
      configuration: $(configuration)

  publish:
   - ^dotnet: publish
     configuration: $(configuration)
