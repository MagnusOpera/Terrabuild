extensions:
  - (dotnet): project3.csproj

steps:
  build:
    - (shell): echo
      args: "building project3"
    - (dotnet): build
      configuration: $(configuration)

  publish:
   - (dotnet): publish
     configuration: $(configuration)
