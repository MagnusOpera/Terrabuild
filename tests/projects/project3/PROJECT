extensions:
  - (dotnet): 1.0.0
    project: project3.csproj

steps:
  build:
    - (shell): echo
      args: "building project3"
    - (dotnet): build
      configuration: $(configuration)

  publish:
   - (dotnet): publish
     configuration: $(configuration)
