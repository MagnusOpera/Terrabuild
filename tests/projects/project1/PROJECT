dependencies:
  - ../../libraries/library1

outputs:
  - dist

targets:
  build:
    steps:
      - echo "building project1"
      - mkdir dist
      - touch dist/toto

  publish:
    depends-on: [ build ]
    steps:
      - echo "publishing project1"

tags:
  - project
