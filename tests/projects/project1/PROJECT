dependencies:
  - ../../libraries/library1

outputs:
  - dist

targets:
  publish: [ build ]

steps:
  build:
    - (shell): echo
      args: "building project1"
    - (shell): make
      args: build

  publish:
    - (shell): echo
      args: "publishing project1"
    - (shell): make
      args: publish

tags:
  - project
