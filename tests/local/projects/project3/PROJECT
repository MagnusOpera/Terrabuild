extensions:
  - (dotnet): project3.csproj
  - (docker): Dockerfile

steps:
  build:
    - (dotnet): build
      configuration: $(configuration)

  publish:
   - (dotnet): publish
     configuration: $(configuration)

  docker:
   - (docker): build
     image: $(image)
     $configuration: $(configuration)

  push:
   - (docker): push
     image: $(image)
     tag: 666

variables:
  image: ghcr.io/magnus-opera/project3
