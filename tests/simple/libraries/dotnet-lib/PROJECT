builders:
  shell:
  netbuild:
    use: dotnet
    with: dotnet-lib.csproj

steps:
  build:
    - (shell): sleep
      arguments: "1"
    - (netbuild): build
      configuration: $(configuration)
    - (shell): echo
      arguments: "done lib2"
