extension @dotnet { }

configuration {
  parser "@dotnet"
}

target build {
  @shell exec { command: "sleep"
               arguments: "1" }

  @dotnet build { configuration: $configuration }

  @shell echo { message: "done lib2" }
}
