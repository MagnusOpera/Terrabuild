builders:
  netbuild:
    use: dotnet
    with: library2.csproj

steps:
  build:
    - (shell): sleep
      arguments: "1"
    - (netbuild): build
      configuration: $(configuration)
    - (shell): echo
      arguments: "done lib2"
