extensions:
  - (dotnet): library2.csproj

steps:
  build:
    - (dotnet): build
      configuration: $(configuration)
    - (shell): sleep
      arguments: "10"
    - (shell): echo
      arguments: "done lib2"
