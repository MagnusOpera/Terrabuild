extensions:
  - (dotnet): library2.csproj

steps:
  build:
    - (shell): sleep
      arguments: "1"
    - (dotnet): build
      configuration: $(configuration)
    - (shell): echo
      arguments: "done lib2"
