extension dotnet {
    script = "../../scripts/dotnet.fsx"
}

extension docker {
    script = "../../scripts/docker.fsx"
    parameters {
        image: "ghcr.io/magnusopera/dotnet-app"
    }
}

configuration {
    labels = [ "app"
               "dotnet" ]
}

target build {
    dotnet build { }
}

target dist {
    dotnet publish { }
}

target docker {
    docker build { }
}

target push {
    docker push { }
}
