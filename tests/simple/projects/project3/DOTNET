extensions:
  - (dotnet): project3.csproj

ignores:
  - Dockerfile
  - DOCKER

steps:
  build:
    - (dotnet): build
      configuration: $(configuration)

  publish:
   - (dotnet): publish
     configuration: $(configuration)
