builders:
  make:
    with: Makefile

dependencies:
  - ../../libraries/shell-lib

outputs:
  - dist

targets:
  publish: [ dist ]

steps:
  build:
    - (echo): "building project1"
    - (make): build
      parameters:
        secret: $(secret_message)

  publish:
    - (shell): echo
      arguments: "publishing project1"
    - (shell): make
      arguments: publish

labels:
  - app
