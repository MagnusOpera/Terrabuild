
configuration {
    dependencies = [ "../../projects/dotnet-app" ]
    ignores = [ "terrabuild.tfstate" ]
    labels = [ "infra" ]
}

target build {
    @terraform plan { }
}

target deploy {
    @terraform apply { }
}
